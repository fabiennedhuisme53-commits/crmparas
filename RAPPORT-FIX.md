# 🛠️ تقرير إصلاح مشكل فقدان البيانات — Paraveda CRM (v3.1 FINAL)

> **الحزمة النهائية للرفع: `Paraveda-deploy-FINAL.zip`** — فيها `index.html` + `api.php` + `INSTALL.txt` (دليل مختصر بالدارجة داخل الحزمة نفسها).

## 1) شنو وقع بالضبط؟ (التشخيص)

المشكل ماشي ف "15 ساعة" — الوقت ما عندو حتى علاقة. المشكل كاين ف **طريقة المزامنة (Sync)** فالتطبيق نفسو.

### كيفاش خدام النظام
- التطبيق (index.html) كيخزن **كولشي** ف `localStorage` ديال المتصفح (اليوزرات، الطلبيات، الشات...).
- كل تعديل كيترسل للسيرفر (`api.php`) اللي كيخزن كلشي ف **ملف واحد**: `crm_data.json`.
- القاعدة: *"آخر واحد كيكتب هو اللي كيسود"* (Last-Write-Wins حسب ساعة الجهاز).

### الجريمة 🔍
التطبيق فيه **بيانات مصنع مضمّنة** (Factory Seed):
- يوزر واحد: `admin@paraveda.ma / admin123`
- **168 طلبية ديمو** (Saad، OUJDA، نظارة القراءة...)
- أسماء الوكلاء الافتراضية (Meryam، imane، AYA...)

وكل متصفح **فارغ الـ localStorage** (جهاز جديد، متصفح آخر، مسح بيانات التصفح، Private Window، أو الضغط على زر "🛠️ الإصلاح") كيدير هاد الشي عند الفتح:

```js
// من الكود (فك التشويش):
const k4 = [{id:1, username:"admin@paraveda.ma", password:"admin123", role:"admin"}]
// عند الفتح: ما كاينش users ف localStorage؟ → استعمل المصنع
useEffect(() => {
  localStorage.setItem(users, JSON.stringify(n));  // كتب المصنع
  aa("afrizon_users_v1");                          // وأرسلو للسيرفر!
}, [n])
```

والأخطر: التطبيق كيرسل حتى **قبل** ما يكمل التحميل الأول من السيرفر (كيتزنا فقط 2.5 ثانية — وإذا كان الهوست بطيء لحظة، التطبيق يبدا ببيانات المصنع ويرسلهم فوق الداتا الحقيقية).

**النتيجة:** أول متصفح/جهاز "نظيف" فتح الـ CRM → مسح الـ 6 يوزرات وبعث الـ 168 طلبية الديمو → الجميع لقا الداتا القديمة رجعت. هادشي لي وقع ليك.

## 2) الحل (فعّال فطبقتين)

### أ) الواجهة (index.html) — 3 رقعات جراحية دقيقة
| الرقعة | شنو كتدير |
|---|---|
| **P1** | أي إرسال للسيرفر كيتأجل **حتى تنجح المزامنة الأولى** → المستحيل يرسل بيانات المصنع فوق داتا السيرفر |
| **P2** | المزامنة الأولى: كتعاود كل 5 ثواني إذا فشلت + كتحدّث الواجهة مباشرة (كما ديال poll) |
| **P3** | قائمة اليوزرات كتقرا من السيرفر بعد المزامنة (ما تبقاش عالقة ف [admin]) |

### أ.2) الواجهة (v2.2 لايف) — 5 رقعات إضافية
| الرقعة | شنو كتدير |
|---|---|
| **P5** 🔴 | **القائمة الخاوية ديال الطلبيات ولّات كتحترم** — قبل: `A.length` كتعتبر 0 طلبية = "ماكاين والو" → يرجع 168 ديمو عند كل متصفح جديد (هادي هي "الطلبيات الوهمية كترجع") |
| **P4** | تحديث لايف للطلبيات + أسماء الوكلاء + الهيستوري (ما كان عندهم حتى آلية تحديث أصلاً) |
| **P6** | المزامنة الخلفية 12s → **5s** (لايف أسرع) |
| **P7/P8** | تحديث لايف للتابات، الأوراق المخصصة، و صور الفريق |

**إثبات E2E (اختبار التطبيق الحقيقي كامل فمتصفح وهمي):** متصفح جديد فارغ + سيرفر فيه 0 طلبية و7 يوزرات → الداتا المحلية ولّات هي ديال السيرفر، **الديمو عمرو أترسل للسيرفر**، الدخول بمستخدم من السيرفر نجح (كان مستحيل)، وطلبية جديدة وصلت **لايف** بلا إعادة تحميل → **9/9 ✔**

### ب) السيرفر (api.php v2) — نفس الواجهة، محصّن بالكامل
| الميزة | الفايدة |
|---|---|
| 🔒 **حارس الأشباح (Ghost Guard)** | يرفض بالضبط بيانات المصنع إذا جات فوق داتا موجودة + يرفض أي نسخة قديمة **مطابقة** ترجع بعد 30 دقيقة — سيناريو الحادثة مستحيل يوقع |
| 💾 **نسخ احتياطية تلقائية** | نسخة قبل كل تعديل (60) + لقطة يومية (45) — حتى لو وقع شي خراب، كولشي قابل للاسترجاع |
| ♻️ **استرجاع تلقائي** | إذا تلف أو تمسح `crm_data.json` → يرجع آخر نسخة سليمة **تلقائياً** |
| 📏 **سقف حجم النسخ (256MB)** | ما كاين حتى خطر من امتلاء القرص مع مرور السنوات — الأقدم كيتحذف أوتوماتيكياً |
| 🧷 **منع التبديل الصامت** | إلا فقد مجلد التخزين صلاحية الكتابة → خطأ صريح بدل ما يبان أن الداتا "اختفت" |
| ↩️ **استرجاع ذكي** | ملي تسترجع نسخة، جميع الأجهزة كياخدوها أوتوماتيكياً فأقرب مزامنة |
| ✍️ **كتابة ذرّية + قفل** | `tmp + rename` + `flock` → مستحيل يبقى الملف فاسد/فارغ (كانت سبباً محتملاً أيضاً) |
| 📜 **سجل تدقيق (audit.log)** | كل كتابة: من أي IP، أي متصفح، أي مفتاح، أي وقت — تعرف بالضبط شنو وقع |
| 🩺 **صفحة تشخيص** | `api.php?action=status&token=paraveda-2026-sync` |
| 📁 **تخزين خارج مجلد الموقع** | البيانات كتتحط فوق `public_html` → إعادة رفع الموقع ما كتمسهاش (+ ترحيل تلقائي للملف القديم) |
| ⏰ **تثبيت ساعة الأجهزة** | جهاز بساعة خاطئة ما يقدرش يخرب الترتيب |
| ↩️ **أداة استرجاع** | `POST {"action":"restore","file":"..."}` لرجوع أي نسخة احتياطية |

### أ.5.7) v3.1 — DARK PRO: التحويل الجذري الكامل
Executive Dark كامل: إعادة تعريف متغيرات الأسطح (slate-50..300 + كل الألوان الناعمة 50/100/200 → داكن شفاف) + طبقة تحويل شاملة (نصوص فاتحة، أسطح navy، hovers، حقول، جداول، هيدر زجاجي داكن، خلفية Aurora، الآلة الحاسبة داكنة) — 112 قاعدة CSS، صفر JS. الاختبارات: 57+22+14+12 = 105 أخضر.

### أ.5.8) v3.0 — PRO THEME: تبديل الشكل الكامل
طبقة Theme كاملة (CSS + خطوط) فوق التطبيق: إعادة تعريف متغيرات الألوان ديال Tailwind (كتعيد تلوين كل صفحة و قائمة دفعة وحدة) + Tajawal/Inter + هيدر زجاجي + ظلال و بطاقات و جداول و أزرار و سكرولبار و انتقالات — بلا أي تغيير فالـ JS (صفر خطر على الداتا). الاختبارات كاملة خضراء: 57+22+14+12.

### أ.5.9) v2.9 — Dashboard performance بتاريخ واحد
حيّدنا خانة 📅 Date من فورم الإضافة (التاريخ كيتسجل أوتوماتيكياً = اليوم عبر vj(i,u,m||f)) + الفورم ولّي 3 أعمدة + تحديث النصوص الإرشادية. كولونة Date فالجدول هي التاريخ الوحيد الظاهر. E2E: 22/22.

### أ.6.0) v2.8 — عمود DATE واحد فقط
تحيّد كولونة "date" (تاريخ التأكيد) من جدول الطلبيات — بقات غير "DATE". الداتا كتبقى كتسجل أوتوماتيكياً فالخلفية (صفحة suivi confirmation و التأكييدات خدامين عادي). E2E: 21/21.

### أ.6.1) v2.7 — إعادة تصميم الآلة الحاسبة باحترافية
سبب الشكل القديم: `grid-cols-4` ماكينش فالـ Tailwind المجمّع (الـ stylesheet تcompile مسبقاً) → الأزرار تجمعو فعمود. الحل: CSS مخصص كامل (`pv-*`) مضاف للـ stylesheet + إعادة كتابة PVCalc: شاشة داكنة بأرقام مفصولة بالآلاف، أزرار بضغط حي، العملية النشطة كتضوي، history بزر مسح، و كل الميزات السابقة (كيبورد + mirror). E2E: 21/21.

### أ.6) الجديد فـ v2.6 — آلة حاسبة 🧮
زر فالـ header فوق يمين (جنب البروفيل، للأدمين والبنات) كيفتح آلة حاسبة احترافية: شاشة monospace كبيرة + سطر العملية، كل العمليات (% ± ⌫ C)، شريط محفوظات آخر 8 عمليات، دعم كيبورد كامل (Enter/Backspace/Escape)، و حالة محفوظة عبر module-mirror (كتقاوم remounts ديال الـ header). E2E: 21/21 (فتح، 7×8=56، محفوظات، إغلاق بـ Escape).

### أ.5) الجديد فـ v2.5 — صفحة Ranking احترافية
أعيد تصميم "Classement des Filles" بالكامل (نفس الحسابات، layout جديد): 4 بطاقات KPI (Total Confirmés/CA/Taux/Objectif مع قابلية تعديل الهدف) + منصة تتويج محسّنة (Livrées/Retours + التطور) + جدول شامل لكل الفتيات بميداليات 🥇🥈🥉 وأعمدة Livrées/Retours/Taux ملون/Montant/Progression + صف TOTAL ÉQUIPE. تحقق E2E: الصفحة كتفتح فالتطبيق الحقيقي و كل العناصر كيبانو (16/16).

### أ.4) الجديد فـ v2.4 — بداية نظيفة + حفظ تلقائي (بحال Google Sheets)
| التغيير | التفاصيل |
|---|---|
| ☠️ **بيانات الديمو ماتت من المصدر** | `dp()` كترجع `[]` بدل 168 طلبية ديمو — CRM كيبدا خاوي و الديمو مستحيل يظهر من التطبيق |
| 🧹 **مسح تلقائي فالسيرفر** | عند أول تشغيل بعد التركيب: إلا لقى السيرفر الطلبيات = الـ seed التجريبي **بالتمام** → يعوضها بقائمة فارغة (و مرة واحدة فقط). البيانات المختلطة بالحقيقي لا تُمس |
| 🚫 **زر Reset محيود + معطّل** | هو كان السبب الخفي: كيرجع لـ checkpoint قديم، و إلا ماكاينش → **كيرجع 168 طلبية ديمو**! الزر تمحى من الواجهة و الدالة تعطلت |
| 💾 **زر Save CMD / Save محيودين** | ما بقاش فيهم أي حاجة — كلشي تلقائي |
| ⚡ **حفظ تلقائي كامل** | أي كتابة/تعديل كيتسجل فوراً (محلي + سيرفر + لايف عند الجميع)، و نقطة الاسترجاع كتحدث وحدها 3 ثواني بعد أي تغيير (badge "✓ محفوظ") |

**التحقق:** 92 اختبار أخضر — سيرفر 54/54 (فيهم 4 جديدة للمسح التلقائي: wipe مرة واحدة، t مرفوع باش الكل يتبناه، الداتا المختلطة ماكتسنسش)، E2E 12/12، مزامنة 12/12، وحدة 14/14 (dp()=[]، reset معطلة، الأزرار محيودة، الحفظ التلقائي موجود).

### أ.3) الميزات الجديدة (v2.3) — بطلب الفريق
| الميزة | التفاصيل |
|---|---|
| 🗑️ **الحذف الجماعي للطلبيات** | الزر الأحمر "حذف المحدد (N)" فشريط أدوات التحديد (حدا نسخ لديجيلوك/نسخ الكل): حدد بالشيكبوكس ولا "تحديد الكل" (كيحدد كل نتائج الفلتر) → تأكيد واحد → كيمسحهم كاملين مع التسجيل فالهيستوري + مزامنة لايف. الطلبيات "Livrée" محمية لغير الأدمين كما كان |
| 🔒 **حماية الصفحات الرئيسية** | 9 صفحات أساسية بلا علامة مسح (✕) وما يمكنش تُمسح حتى بالخطأ: COMONDES، Dashboard performance، PRODUITS، suivi confirmation، pièce، statistique، suivi rentabilité، Les villes، Work Times. الصفحات الاختيارية (LES RENV، LES OBJECTIFS، RECLAMATION، Sheet129 + صفحات البنات) بقات قابلة للمسح |

**التحقق:** اختبار وحدة للكود المُحقَن كما هو فالملف (10/10 ✔) + فحص DOM حقيقي فالـ E2E: 9/9 تابات محمية بلا ✕ و4 اختيارية فقط بقاو بـ✕ (12/12 ✔).

## 3) طريقة التركيب (5 دقايق)

1. فك `Paraveda-deploy-FIXED.zip`
2. أرفع **الملفين** فوق القديمين فالهوست: `index.html` و `api.php`
3. غادي يلقى `crm_data.json` القديم و **يهاجرُو تلقائياً** للمكان الجديد الآمن
4. قول لكل الفريق يديرو **Ctrl+F5** (تحديث قاسي) مرة وحدة

### التحقق أن كل شيء خدام
افتح فالمتصفح:
```
https://الدومين-ديالك/api.php?action=status&token=c6e04cb5de9088be01a685abc243995a80426eba45de2060
```
غادي تشوف: حالة التخزين، عدد المفاتيح، آخر كتابة، النسخ الاحتياطية، وآخر السجل.
⚠️ ملاحظة مهمة: `install.id` خاصو يبقى نفسو — **إلا تبدل فهاد معناها أن الهوست كيمسح الملفات**، وقتها خاصنا نتحولو لقاعدة بيانات MySQL (عندك ف cPanel غالباً).

### استرجاع نسخة احتياطية
```
GET  .../api.php?action=backups&token=c6e04cb5de9088be01a685abc243995a80426eba45de2060   ← قائمة النسخ
POST .../api.php   Headers: X-Sync-Token: c6e04cb5de9088be01a685abc243995a80426eba45de2060
Body: {"action":"restore","file":"b-20260823-153000-123.json"}
```

## 4) الاختبارات

- **50 اختبار سيرفر** (PHP حقيقي 8.3): سيناريو الحادثة كامل (بجيلي المتصفحات القديمة والجديدة) + الأشباح + الفساد + المسح + الاسترجاع (مع تبني كل الأجهزة له) + الترحيل + رفض التوكن القديم + سقف حجم النسخ + الأمان → **50/50 ✔**
- **12 اختبار واجهة** (الكود المعدل بـ fetch و localStorage وهميين): seed مستحيل يوصل للسيرفر قبل المزامنة، إعادة المحاولة، التدفق العادي، التوكن الجديد → **12/12 ✔**
- المجموع: **62/62 ✔** + تحقق الـ hash أن الحزمة النهائية فيها بالضبط نفس الملفات المختبرة

## 4.5) 📌 خطوات ما بعد الرفع (مهمة!)

1. إلا الهوست فيه الداتا القديمة (الحالة المرجوعة بالخطأ): دخل بكلمة السر القديمة **`admin123`** (الداتا القديمة هي اللي كتحكم)، وبدلها فوراً من التطبيق.
2. مسح الـ 168 طلبية ديمو و إعادة زيادة الـ 6 يوزرات — دابا **آمنة 100%**، حتى متصفح جديد ما يقدر يمسحهم.
3. تتبع `install.id` فصفحة status: إلا تبدل من بعد → الهوست كيمسح الملفات → قولها ليا و نحولو ل MySQL.

## 5) 🔐 التعليمات السرية الجديدة (مدمجة فهاد النسخة)

| الشيء | القيمة الجديدة |
|---|---|
| **كلمة سر الأدمن** (الافتراضية للمتصفحات الجديدة) | `rOYtKv0cd9UW` |
| **توكن المزامنة** (مربع ف index.html و api.php معاً) | `c6e04cb5de9088be01a685abc243995a80426eba45de2060` |

⚠️ ملاحظات:
- إلا كان الهوست ديالك عندو بيانات قديمة (بكلمة `admin123`)، دخل بها مرة و بدّلها من داخل التطبيق (غيّر كلمة السر)، أو استعمل `restore` باش ترجع داتا نظيفة.
- المتصفحات اللي عندها كاش قديم (بالتوكن القديم) غادي تترفض كتاباتهم (403) حتى يديرو **Ctrl+F5** — ما كاين حتى خطر على الداتا.
- حافظ على هاد الملف فمكان آمن — و إلا المستودع ديالك عمومي (public)، بدّل التوكن مرة أخرى.

## 6) توصيات للمستقبل (باش وليتي محترف أكثر)

1. **بدّل كلمة سر الأدمن من داخل التطبيق** من بعد أول دخول (الافتراضية غير للبداية) — والتوكن إلا بغيتي تولي بدلو دورياً.
2. إذا كبر الفريق: تحول لـ **MySQL** (cPanel ديالك فيه) — نقدر نديرها ليك فمرحلة جاية.
3. زر "🛠️ الإصلاح (مسح الكاش)" بقى آمن دابا، ولكن ما تستعملوش إلا إذا ضروري.
4. نسخة يومية خارج الهوست (الـ backups كاينين، ولكن الهوست نفسو ما يبقىش مصدر وحيد).

---
*الحزمة النهائية: `Paraveda-deploy-FINAL.zip` — المصدر: `app/index.html` (معدل) + `app/api.php` (v2.1) — الاختبارات: `tests/` (50 سيرفر + 12 واجهة) — أدوات: `tools/`*
